import { createStyles } from '@mantine/core';

export default createStyles(() => ({
  root: {
    display: 'flex',
    alignItems: 'center',
  },

  inner: {
    display: 'flex',
    alignItems: 'center',
  },
}));
